@Pipe({
  name: 'domain'
})
export class DomainPipe implements PipeTransform {

  transform(value: string, name: string, domains: Domain[]): any {
    return domains.filter(d => d.name === name && d.wert === value).map(d => d.bezeichnung)[0];
   }

}
